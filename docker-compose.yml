services:
  web:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=production
    depends_on:
      - redis

  redis:
    image: "redis:alpine"
    restart: unless-stopped
